function predictLinearRegr(inputValue) {
    // Thay thế đoạn mã dưới đây bằng nội dung của file linear_regr_model.py
    // var linearRegrModelCode = "YOUR_LINEAR_REGR_MODEL_CODE";
    // eval(linearRegrModelCode);
    
    // Gọi hàm dự đoán từ mô hình đã chuyển đổi
    var prediction = predict(inputValue);
    return prediction;
}
